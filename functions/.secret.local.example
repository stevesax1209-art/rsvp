# Local emulator secret file — copy this to .secret.local and fill in your key.
# Used only by `firebase emulators:start`. Never deployed or committed.
MAILERLITE_API_KEY=your_mailerlite_api_key_here
